<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
>

    <item android:title="Plus"
          android:id="@+id/plus"
          app:showAsAction="always"
    android:icon="@drawable/add"/>
    <item android:title="Submission Reminder"
    android:id="@+id/subrem"/>
    <item android:title="Project Reminder"
    android:id="@+id/projrem"/>
    <item android:title="Help"
    android:id="@+id/help"/>
    <item android:title="Refresh"
    app:showAsAction="always"
          android:id="@+id/refresh"
    android:icon="@drawable/baseline_refresh_24"/>

</menu>